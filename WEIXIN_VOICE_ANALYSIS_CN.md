# 微信语音分析报告

## 目标

理解为什么 Hermes / iLink 微信语音回复无法作为原生微信语音气泡到达，以及将问题简化为可复现的工程结论。

## 范围

本分析涵盖了：

1. 本仓库中的辅助项目
2. Hermes 微信源代码（`~/.hermes/hermes-agent`）
3. 从本地音频到 iLink `sendmessage` 的完整出站媒体链路

本分析**未尝试**逆向微信私有协议或修改微信官方客户端二进制。

## 追踪过程

### 1. Hermes 辅助项目

我们添加并验证了：

1. 从文本或本地音频生成独立的 `.silk` 文件
2. 原生 `.silk` 实验发送器
3. 可靠的音频文件附件发送器
4. 本地 Hermes / iLink 账号状态诊断工具

### 2. Hermes 微信源代码

我们确认 Hermes 源代码在 `gateway/platforms/weixin.py` 中仍然保留了原生出站语音气泡逻辑。

重要观察：

1. `send_voice()` 被硬编码为调用 `_send_file(... force_file_attachment=True)`
2. `_outbound_media_builder()` 仍然有活跃的 `.silk -> MEDIA_VOICE / ITEM_VOICE` 分支
3. 这意味着原生语音气泡代码仍然存在，但从正常的 `send_voice()` 路径无法到达

### 3. 真实媒体行为

我们验证了：

1. 生成的 `.silk` 文件是合法的腾讯风格 `SILK_V3` 文件
2. 原生 `ITEM_VOICE` 负载构造正确
3. 音频文件附件能到达目标对话
4. 原生语音发送可以报告成功，但不在个人微信客户端中出现

## 关键发现

### 发现 A：本地编码不是主要瓶颈

本项目生成的 `.silk` 文件是真实的、非空的、结构正确的。

观察到的指标：

1. 合法的 `SILK_V3` 魔数头
2. 预期的音频时长
3. 稳定的文件大小和 md5

### 发现 B：Hermes 源代码仍然支持原生语音负载构造

相关的 Hermes 代码路径仍然存在：

1. `.silk`
2. `MEDIA_VOICE`
3. `ITEM_VOICE`
4. `voice_item`

这意味着问题不是"Hermes 从未实现原生语音气泡"。

### 发现 C：旧的备选方案是显式的，而不是意外的

Hermes 被强制进入文件附件行为是由于 `send_voice()` 中一个显式的 `force_file_attachment=True` 调用。

该改变禁用了原生分支，尽管分支本身仍然存在。

### 发现 D：原生语音可以在 API 调用成功后仍然消失

这是最重要的结果。

我们进行了一个对照实验：

1. 先发送一条标记性的文本消息
2. 立即发送一条原生语音尝试
3. 标记性文本消息到达了
4. 原生语音根本没有出现

这告诉我们：

1. 对话上下文是有效的
2. 目标对话是有效的
3. 发送路径是活跃的
4. 失败特别发生在原生语音消息上

## 工程结论

当前证据指向公开 iLink Bot 路径中的上游限制或过滤（针对个人微信账号）。

简化后的结论是：

1. 本地音频 → `.silk` 有效
2. `.silk` → `ITEM_VOICE` 负载有效
3. Hermes 能发送请求
4. 个人微信客户端可能仍然不渲染该消息
5. `ITEM_FILE` 音频附件是唯一经过验证的可靠传送方式

## 当前可用功能

克隆本仓库后，经过验证可用的基线功能是：

1. 生成真实的腾讯风格 `.silk` 文件
2. 检查原生语音负载构造
3. 发送可靠的微信音频文件附件
4. 诊断本地 Hermes / iLink 配置

这是该项目今天支持的基本功能。

## 为什么原生语音气泡在今天无法使用

最可能的原因，按可信度排序：

1. 公开 iLink Bot 出站 `ITEM_VOICE` 被 API 基础设施接受，但不被个人微信客户端渲染
2. 公开参考实现暴露了语音负载 Schema，但不保证个人客户端的传送支持
3. 真正的出站语音气泡支持可能需要额外的私有或内部字段
4. 真正的语音气泡支持路径可能仅限于微信官方客户端自行录制并发送消息

## 我们在调查期间的改变

在本辅助仓库中：

1. 添加了从文本和本地音频生成 `.silk` 的功能
2. 添加了原生 `.silk` 实验命令
3. 添加了可靠的音频附件备选命令
4. 添加了围绕语音负载生成和发送行为的测试

在本地 Hermes 源代码中：

1. 追踪了真实的 `send_voice()` 和 `_send_file()` 路径
2. 在实验门后重新启用了原生语音行为
3. 验证了 Hermes 能到达 `.silk` 原生发送路径

## 我们未证明的内容

我们**未证明**公开 iLink Bot 基础设施能成功传送个人微信原生语音气泡。

这仍然未被证明，且目前与观察到的行为相矛盾。

## 推荐的后续步骤

### 实际产品方向

将音频文件附件传送视为当前的稳定功能。

### 协议调试方向

如果原生语音气泡仍然很重要，为以下内容添加门控 Hermes 调试日志：

1. `getuploadurl` 的关键请求字段
2. `sendmessage` 的关键请求字段
3. 返回的 `ret / errcode / errmsg`
4. 发送是 `ITEM_VOICE` 还是 `ITEM_FILE`

这将提供适当的上游证据包。

### 替代实现方向

如果产品需求严格要求"必须显示为微信语音气泡"，最可信的备选路径是微信客户端自动化：

1. 打开真实的微信客户端
2. 进入目标对话
3. 将音频输入到麦克风或虚拟麦克风路径
4. 让客户端自行录制并发送消息

该路径存在平台风险和账户风险的权衡，但它是与个人微信如何正常创建语音气泡直接一致的唯一剩余路由。
